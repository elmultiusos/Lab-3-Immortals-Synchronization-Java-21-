package edu.eci.arsw.core;

public class BlackListServer {
    public boolean isInBlackList(String ipAddress) {
        // Simulación: reemplaza por tu lógica real
        return Math.random() < 0.1; // 10% de probabilidad de "lista negra"
    }
}